---
title: "Introduction"
subtitle: "Analysis of Animal Movement Data with R (together with Brian Smith)"
format: 
  clean-revealjs:
    progress: true
    code-line-numbers: false
author:
  - name: Johannes Signer
    orcid: 0000-0002-1771-7775
    email: jmsigner@gmail.com
    affiliations: University of Göttingen, Germany & Freelancer
date: last-modified
execute:
  echo: true
  warning: false
  message: false
filters: 
  - timer
revealjs-plugins:
  - codefocus
---

```{r setup}
#| include: false
library(tidyverse)

knitr::opts_knit$set(
  root.dir = here::here("01 Introduction/")
)

knitr::opts_chunk$set(
  echo = TRUE,
  warning = FALSE,
  message = FALSE,
  fig.height = 4.5,
  fig.width = 4.5
)
```


# Welcome

## To get started

- Welcome to this online course on animal movement. 
- We are Brian and Johannes.
- Who are you? *Pair up and introduce your partner*

------

### Ask your partner: 

::::: {.coluns}

:::: {.column width = "60%"}

- What is your background?
- Where do you come from?
- Where do you study/work?
- What is your study organism?
- Why do you attend this course?
  
::::

:::: {.column width = "40%"}
  
:::{.timer #timer-1 seconds=180 starton=interaction}
:::

::::
  
:::::
  
## Outline of the course

### Day 1
1. Introduction to movement data (GPS; J)	
2. Introduction to movement data (Argos, Acoustic; B)	
3. Characterizing movement data with steps and turns (J)
4. Movement as RW and CRW	 (B)

::: {.fragment}

### Day 2
5. Detecting and handling outliers	(B)
6. Home ranges concepts and tools and aKDE	(B)
7. Handling multiple animals (J)

:::

------------

### Day 3

8. Working with home ranges (J)
9. Using statistical distribution to describe movement	(J)
10.	Outlook: RSA (J), SSA (B), HMMs (J), Migration Mapping (B)	
11. Q&A & own Data

::: {.fragment}
**Throughout the whole course and also for the exercises, if possible, please use your own data. **
:::

--- 

## Some logistics

- Three days (Monday to Wednesday) from 14:00 to 18:00 Berlin time.
- We split these 4-hour block into 3 - 4 chunks, each roughly structured like this: 
  - Lecture ~ 30 min
  - R walkthrough ~ 20 min
  - Introduction of exercises ~ 5 min
- A 20 min break approx. from 15:50 to 16:10.

------
    
- Lectures will be held via zoom.
- During the whole workshop we have a slack channel where you ask questions (we will monitor the channel during the course, feel free to ask questions there also outside the course hours).

- All course material is available here: <https://github.com/jmsigner/movement_ecology_1>

::: {.notes}
Say that you can download the whole repo in one go or clone it. 
:::

## Packages that we will use

- We will mainly use the `amt` package, but also occasionally other packages for movement analysis. 

- See also `required_packages.R` for a list of all packages that we need and to get the latest version of all packages. 

# Some conventions in R

- `%>%` or `|>`: Pipes the output of one function to a next function. We will discuss this further later on. 
- `::` to access a namespace form a package. 
- `data.frame` or `tibble`?

::: {.fragment}

- We often use `here::here("path to a file")`, when reading in a file. 
- The first `here` calls the function `here()` from the package `here`. 
- The function `here()` dynamically creates the absolute path to the project root. 

```{r}
here::here()
```

:::: {.notes}
This means, even though our computers differ, relative to the project root, we all have the same directory structure. 
::::

:::

# Movement data (in R)

## Typical steps

1. Preprocessing: getting data ready into x, y, t
2. Postprocessing: removing outliers
3. Analysis: doing the actual analysis

::: {.notes}
1. Preprocessing: E.g., Global Location Sensors only provide light intensity. Brian will talk about this a bit next. 
2. We will talk about this tomorrow (-> how to find locations that are unreasonable). 
3. Home ranges etc..
:::

```{r, echo = FALSE, out.width="95%", fig.cap="Figure from Joo et al 2020"}
knitr::include_graphics(here::here("01 Introduction/img/joo2020.png"))
```

For more see [Joo et al. 2020](https://besjournals.onlinelibrary.wiley.com/doi/full/10.1111/1365-2656.13116). 


:::{.notes}
- First we obtain raw data, that is later being processed. 
- Then we use tracking data and so on
- Finally, we want to achieve something. 
:::


## Why do we use R?

- The statistical software package R has become a widely used tool for data analysis in ecology and evolution and also in movement ecology. 
- Also visit R task view for tracking data: https://cran.r-project.org/web/views/Tracking.html




# Geographic data in brief

- Movement data is inherently spatial. 
- Thus we will have to deal with tools to work with spatial data (R has a rich set of tools to deal with spatial data; e.g. <https://geocompr.robinlovelace.net/>).

- We will work frequently with raster data (spatial covariates) and possibly with vector data (i.e., home ranges).

- One of the challenges is to ensure that both -- tracking data and covariates -- have a matching coordinate reference system (CRS). 

---

::: {.incremental}
- The CRS defines the reference system that is being used to explicitly reference a feature in space. 
- There are two classes of CRS: **geographic** (e.g., WGS84) and **projected** (e.g., UTM) CRS. 
- Projected CRS flatten three dimensional data to a two-dimensional plane (and introduce some distortion).
- CRS are often referred to with their EPSG[^1] code.
- EPSG codes are four- to five-digits number that refer to different CRS. See for example <www.epsg.io>.
- Which CRS is best to use? It depends on the range of the study species. I usually prefer projected CRS, because their units are meters and not degrees. 
:::


[^1]:[EPSG stands for European Petroleum Survey Group, who came up with the system]{.fragment}

## Environmental covariates 

In addition to movement data, we often want to annotate them with environmental data often as:

- Vector layers (e.g., road networks, rivers, protected areas)
- Raster layers (e.g., land use, remotely sensed data such as NDVI, climatic variables)

::: {.fragment}
`amt` supports raster layers from the `terra` package. ‚
:::

## Movement data
- Often times the data we receive is just a time series of coordinates (longitude, latitude and time stamp). 
- Depending on the sensors we use, other (meta) information may also be stored (this could include temperature, coordinates in a different [projected] CRS, ...).


::: {.fragment}

```{r, echo = FALSE, out.width="85%", fig.cap=""}
knitr::include_graphics(here::here("01 Introduction/img/relocs.png"))
```

:::


## Reading preprocessed movement data in R

We use the `amt` package, but for many other packages it is the same workflow of:

:::: {.columns}
::: {.column width = "49%" .fragment fragment-index="1"}


- Read the data into R (e.g., with `read.csv()` or alike)
- If needed, parse the date and time. 
- Create an object, that knows your data are points in time and space. 

::: {.callout-warning .fragment fragment-index="3"}
### Multiple individuals
Note, at this point multiple individuals can be mixed and sampling rates can be heterogeneous. 
:::

:::
::: {.column width = "2%" .fragment fragment-index="2"}
:::

::: {.column width = "49%" .fragment}


```{r}
#| out-width: 70%

library(amt)
dat <- read.csv(here::here("data/deer.csv"))
head(dat, n = 2)
dat$t_ <- ymd_hms(dat$t_)
trk <- make_track(dat, x_, y_, t_)
head(trk, n = 2)
class(trk)
```

:::
::::



----

- **Bursts** can be created from tracks. 
- A burst is a sequence of (re)locations from the **same** individual at **equal** time intervals (with some tolerance).

```{r, echo = FALSE, out.width="85%", fig.cap=""}
knitr::include_graphics(here::here("01 Introduction/img/track_burst.png"))
```

## Sampling rates, and resampling


- The function `summarize_sampling_rate()` takes a track as input and gives a summary of the sampling rate.
- If there are multiple animals present, there is also the function `summarize_sampling_rate_many()`, which will do the same thing, but for many animals.

---

-  Once a suitable sampling rate is determined, the function `track_resample()` can be used to take a relocation every predefined time interval (e.g., 30 minutes, 2 hours, ...) within a tolerance.
- The result of `track_resample()` is again a track with one additional column called `burst_`. 

------

If you have gaps and/or different sampling rates, interpolation with continuous time movement models may be an option. 

```{r, echo = FALSE, out.width="85%", fig.cap=""}
knitr::include_graphics(here::here("01 Introduction/img/jonsen2023_mee.png"))
```



## Data sets that we use

- We will use several data sets during this course including a data set on fishers, elephants and deer. 
- Feel free to use your own data during the exercises, we are happy to help to get it into shape. 
- For the R walkthrough we also simulate data. 


